[project]
name = "af-component-evaluation"
version = "11.10.36"
description = "Batch evaluation framework for DataRobot agents using NeMo Evaluator"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jinja-cli",
]

[tool.uv]
# cve-sync:begin
# cve-sync: this root resolves none of the policy's floors. The gate reads uv.lock, not this block.
# cve-sync:end
